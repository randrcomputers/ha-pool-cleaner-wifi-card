Copy the two PNG files from **this GitHub folder** into **`config/www/pool_card/`** on your Home Assistant host (SSH, Samba, or Studio Code Server).

Example URLs in the card:

- **Robot (cleaner):** `/local/pool_card/robot_triton_front.png`
- **Power supply:** `/local/pool_card/psu_front.png`
